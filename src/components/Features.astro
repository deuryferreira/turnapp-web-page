---
import { Image } from "astro:assets";
import { Icon } from "astro-icon/components";
import featureImage1 from "../assets/feature-1.svg"; // Asumiendo que usarás imágenes que muestren control/pantalla
import featureImage2 from "../assets/feature-2.svg"; // Asumiendo que usarás imágenes que muestren el análisis
import featureImage3 from "../assets/feature-3.svg"; // Asumiendo que usarás imágenes que muestren el kiosco/impresión

const features = [
  {
    title: "Digitaliza el Flujo Completo",
    description:
      "Elimina las filas caóticas y gestiona el ciclo del turno de principio a fin. Desde la emisión (autoservicio o recepción) hasta el llamado final, todo está centralizado en TurnApp.",
    icon: "ph:star-duotone", // Icono de estrella/calidad
    image: featureImage1,
  },
  {
    title: "Optimiza la Productividad por Área",
    description:
      "Accede a un flujo controlado por servicio o área, con llamados automatizados que garantizan la metodología PEPS (Primero en Entrar – Primero en Salir). Menos errores, más eficiencia.",
    icon: "ph:lightning-duotone", // Icono de velocidad/eficiencia
    image: featureImage2,
  },
  {
    title: "Experiencia Clara en Tiempo Real",
    description:
      "Ofrece una experiencia profesional y fluida. Utiliza la pantalla HUB personalizable para el llamado visible en tiempo real, reforzado con notificación de audio y voz automática.",
    icon: "ph:globe-duotone", // Icono de visibilidad/alcance
    image: featureImage3,
  },
];

// --- Texto Principal ---
const mainTitle = "Todo lo que necesitas para controlar tu flujo de atención";
const mainSubtitle =
  "Nuestras herramientas combinan la robustez B2B con una interfaz intuitiva. Digitaliza rápidamente tu servicio y asegura una experiencia de cliente de alto nivel.";
// -----------------------
---

<section
  class="py-20 bg-gradient-to-b from-white to-payflo-gray relative overflow-hidden"
>
  <div class="absolute inset-0 opacity-5 parallax" data-speed="0.1">
    <div
      class="absolute inset-0 bg-repeat"
      style="background-image: url('/grid-pattern.svg');"
    >
    </div>
  </div>

  <div class="container-custom relative">
    <div class="text-center max-w-3xl mx-auto">
      <h2 class="animate-on-scroll delay-0 text-3xl md:text-4xl font-bold">
        {mainTitle}
      </h2>
      <p class="animate-on-scroll delay-1 mt-4 text-xl text-gray-600">
        {mainSubtitle}
      </p>
    </div>

    <div class="mt-16 grid grid-cols-1 md:grid-cols-3 gap-8">
      {
        features.map((feature, index) => (
          <div
            class={`feature-card animate-on-scroll delay-${index + 2} group`}
          >
            <div class="h-12 w-12 rounded-lg bg-gradient-to-r from-payflo-purple to-payflo-blue flex items-center justify-center text-white mb-6 transition-transform duration-300 group-hover:scale-110">
              <Icon
                name={feature.icon}
                class="h-6 w-6 transition-transform duration-300 group-hover:rotate-12"
              />
            </div>
            <h3 class="text-xl font-semibold transition-colors duration-300 group-hover:text-payflo-purple">
              {feature.title}
            </h3>
            <p class="mt-3 text-gray-600">{feature.description}</p>
            <div class="mt-6 hover-zoom rounded-lg overflow-hidden">
              <Image
                src={feature.image}
                alt={feature.title}
                class="w-full h-auto rounded-lg transition-all duration-500"
                width={300}
                height={200}
              />
            </div>
          </div>
        ))
      }
    </div>
  </div>
</section>
