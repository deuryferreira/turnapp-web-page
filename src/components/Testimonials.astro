---
import { Image } from "astro:assets";

const testimonials = [
  {
    // Énfasis en la eliminación del caos y el orden
    quote:
      "La implementación de TurnApp fue clave para terminar con el caos en la recepción. Ahora tenemos un flujo de atención ordenado y medible, mejorando la moral del personal.",
    author: "Elena Ramírez",
    role: "Gerente de Operaciones, Clínica Central",
    avatar: "/testimonial-avatar-1.jpg", // Manteniendo el placeholder
  },
  {
    // Énfasis en la profesionalidad y la rapidez de implementación/soporte
    quote:
      "El equipo de TurnApp nos guió en cada paso. Pudimos personalizar las pantallas con nuestra marca en muy poco tiempo. El soporte ha sido excepcional desde el día uno.",
    author: "Javier Soto",
    role: "Director de TI, Banco del Pueblo",
    avatar: "/testimonial-avatar-2.jpg", // Manteniendo el placeholder
  },
  {
    // Énfasis en el ROI: reducción de tiempos de espera y satisfacción del cliente
    quote:
      "Nuestros tiempos de espera se redujeron en un 30% en el primer mes. La claridad que ofrecen las pantallas ha sido un cambio radical para la satisfacción de nuestros clientes.",
    author: "Lorena Guzmán",
    role: "Jefa de Servicio al Cliente, Agencia Gubernamental",
    avatar: "/testimonial-avatar-3.jpg", // Manteniendo el placeholder
  },
];

// --- Texto Principal ---
const mainTitle = "Contamos con la confianza de empresas líderes";
const mainSubtitle =
  "Vea lo que dicen nuestros clientes sobre la transformación de su gestión de filas con TurnApp.";
// -----------------------
---

<section class="py-20 bg-white relative overflow-hidden">
  <div class="absolute inset-0 opacity-5 parallax" data-speed="0.15">
    <div
      class="absolute inset-0 bg-repeat"
      style="background-image: url('/grid-pattern.svg');"
    >
    </div>
  </div>

  <div class="container-custom relative">
    <div class="text-center max-w-3xl mx-auto animate-on-scroll">
      <h2 class="text-3xl md:text-4xl font-bold">
        {mainTitle}
      </h2>
      <p class="mt-4 text-xl text-gray-600">
        {mainSubtitle}
      </p>
    </div>

    <div class="mt-16 grid grid-cols-1 md:grid-cols-3 gap-8">
      {
        testimonials.map((testimonial, index) => (
          <div
            class="testimonial-card animate-on-scroll group"
            style={`animation-delay: ${index * 0.15}s`}
          >
            <div class="flex items-center mb-6">
              <div class="relative mr-4 overflow-hidden rounded-full transition-all duration-300 group-hover:shadow-md">
                <img
                  src={testimonial.avatar}
                  alt={testimonial.author}
                  class="w-12 h-12 rounded-full transition-transform duration-500 group-hover:scale-110"
                />
              </div>
              <div>
                <h4 class="font-semibold transition-colors duration-300 group-hover:text-payflo-purple">
                  {testimonial.author}
                </h4>
                <p class="text-sm text-gray-600">{testimonial.role}</p>
              </div>
            </div>
            <p class="text-gray-700 italic relative">
              <span class="absolute -top-2 -left-1 text-4xl text-payflo-purple opacity-20 transition-all duration-300 group-hover:opacity-40 group-hover:scale-110">
                "
              </span>
              {testimonial.quote}
              <span class="absolute -bottom-2 -right-1 text-4xl text-payflo-purple opacity-20 transition-all duration-300 group-hover:opacity-40 group-hover:scale-110">
                "
              </span>
            </p>
          </div>
        ))
      }
    </div>
  </div>
</section>
